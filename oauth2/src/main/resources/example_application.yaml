app:
  jwt:
    secret:
    issuer: oauth2-demo
    expiry-seconds: 3600
  google:
    client-id:
  github:
    client-id:
    client-secret:

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/oauth2
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
