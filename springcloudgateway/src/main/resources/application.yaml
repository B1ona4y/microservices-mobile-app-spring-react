spring:
  cloud:
      gateway:
        routes:
          - id: auth-service
            uri: http://oauth2:8080
            predicates: [Path=/auth/**,/users/**]
  application:
    name: springcloudgateway
